{
  "name": "jasonduffett-net-infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AWS CDK app that deploys jasonduffett.net to CloudFront via composureCDK.",
  "scripts": {
    "build": "tsc",
    "prebuild": "rm -rf dist",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "redirects": "tsx ../scripts/build-redirects.ts"
  },
  "devDependencies": {
    "aws-cdk": "^2.170.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "@types/node": "^22.10.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2",
    "@composurecdk/core": "^0.3.0",
    "@composurecdk/s3": "^0.3.0",
    "@composurecdk/cloudfront": "^0.3.0",
    "@composurecdk/sns": "^0.3.0",
    "@composurecdk/cloudformation": "^0.3.0"
  }
}
