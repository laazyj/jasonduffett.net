{
  "name": "@jasonduffett-net/cdk",
  "version": "1.0.0",
  "private": true,
  "description": "AWS CDK infrastructure for jasonduffett.net (domain, hosting, DNS).",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist cdk.out",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:update": "vitest run -u --passWithNoTests",
    "test:watch": "vitest",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all",
    "deploy:stack": "cdk deploy",
    "diff": "cdk diff",
    "check:redirects": "node scripts/check-redirects.mjs",
    "indexnow:ping": "node scripts/indexnow-ping.mjs",
    "smoke": "node scripts/smoke-test.mjs"
  },
  "nx": {
    "implicitDependencies": [
      "@jasonduffett-net/site",
      "@jasonduffett-net/clara",
      "@jasonduffett-net/naomi"
    ],
    "targets": {
      "synth": {
        "dependsOn": [
          "build",
          "^build"
        ],
        "inputs": [
          "default",
          "^default",
          {
            "env": "ALERT_EMAIL"
          },
          {
            "env": "CDK_DEFAULT_ACCOUNT"
          },
          {
            "env": "CDK_DEFAULT_REGION"
          }
        ],
        "outputs": [
          "{projectRoot}/cdk.out"
        ],
        "cache": true
      },
      "deploy": {
        "dependsOn": [
          "build",
          "^build"
        ],
        "cache": false
      },
      "deploy:stack": {
        "dependsOn": [
          "build",
          "^build"
        ],
        "cache": false
      },
      "diff": {
        "dependsOn": [
          "build",
          "^build"
        ],
        "cache": false
      }
    }
  },
  "dependencies": {
    "@composurecdk/acm": "^0.9.5",
    "@composurecdk/budgets": "^0.9.5",
    "@composurecdk/cloudformation": "^0.9.5",
    "@composurecdk/cloudfront": "^0.9.5",
    "@composurecdk/cloudwatch": "^0.9.5",
    "@composurecdk/core": "^0.9.5",
    "@composurecdk/iam": "^0.9.5",
    "@composurecdk/route53": "^0.9.5",
    "@composurecdk/s3": "^0.9.5",
    "@composurecdk/sns": "^0.9.5",
    "aws-cdk-lib": "^2.267.0",
    "constructs": "^10.8.1"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "aws-cdk": "^2.1139.0",
    "vitest": "^4.1.11"
  }
}
