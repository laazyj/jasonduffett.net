# Google Analytics 4 measurement ID for Clara's subsite. This is a SEPARATE GA4
# property from the main site (jasonduffett.net) so the two traffic streams stay
# distinct. When set at build time the GA4 tag and cookie consent banner are
# emitted into the layout. Leave unset for local dev or to disable analytics.
CLARA_GA_MEASUREMENT_ID=
