<!doctype html>
<html lang="{{ site.language }}">
<head>
  {% include "partials/head.njk" %}
</head>
<body>
  <div class="grain" aria-hidden="true"></div>
  <div class="wrap">
    {# The definition belongs to the pages that are about the model. #}
    {% set mastheadLede = (page.url == "/") %}
    {% include "partials/masthead.njk" %}

    {{ content | safe }}

    {% include "partials/site-footer.njk" %}
  </div>
  {% include "partials/consent-banner.njk" %}
</body>
</html>
