{% if analytics.measurementId %}
<aside
  class="consent"
  id="jd-consent"
  role="dialog"
  aria-label="Cookie consent"
  aria-describedby="jd-consent-msg"
  hidden
>
  <p class="consent-msg" id="jd-consent-msg">
    This page uses Google Analytics to count visits. It sets cookies.
    <span class="consent-cta">Your call &mdash; <a href="{{ '/privacy/' | rel }}">what's collected</a>.</span>
  </p>
  <div class="consent-actions">
    <button type="button" class="consent-btn consent-btn--ghost" data-consent="reject">Reject</button>
    <button type="button" class="consent-btn consent-btn--primary" data-consent="accept">Accept</button>
  </div>
</aside>
<script>
  (function () {
    var KEY = "jd-consent-v1";
    var banner = document.getElementById("jd-consent");
    if (!banner) return;
    var stored = null;
    try { stored = localStorage.getItem(KEY); } catch (e) {}
    function show() { banner.hidden = false; }
    function hide() { banner.hidden = true; }
    function set(value) {
      try { localStorage.setItem(KEY, value); } catch (e) {}
      if (value === "granted" && typeof gtag === "function") {
        gtag("consent", "update", { analytics_storage: "granted" });
      }
      hide();
    }
    banner.addEventListener("click", function (e) {
      var t = e.target.closest("[data-consent]");
      if (!t) return;
      set(t.dataset.consent === "accept" ? "granted" : "denied");
    });
    document.addEventListener("keydown", function (e) {
      if (e.key === "Escape" && !banner.hidden) set("denied");
    });
    document.addEventListener("click", function (e) {
      var t = e.target.closest("[data-consent-reopen]");
      if (!t) return;
      e.preventDefault();
      show();
    });
    if (stored !== "granted" && stored !== "denied") show();
  })();
</script>
{% endif %}
