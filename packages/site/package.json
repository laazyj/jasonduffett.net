{
  "name": "@jasonduffett-net/site",
  "version": "0.0.0",
  "private": true,
  "description": "Blog and landing site for jasonduffett.net (Eleventy).",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "eleventy",
    "start": "eleventy --serve --quiet"
  },
  "nx": {
    "targets": {
      "start": {
        "cache": false
      }
    }
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-rss": "^2.0.2",
    "@11ty/eleventy-plugin-syntaxhighlight": "^5.0.0"
  }
}
